# PURE Fitness HTML fixtures

These files are small, sanitized representations of the public club index and
bilingual club pages. They preserve only the HTML structures used by the
importer: links, metadata, JSON-LD, contact details, and amenity sections.

They contain no production credentials and tests must not replace them with
live network responses.
